#include "main.h"

/**
 * mul - a function to multiply two integers
 * @a: first input
 * @b: second input
 * Return: Always 0 (success)
 */

int mul(int a, int b)
{
	{
		int mul = a * b;

		return (mul);
	}
	return (0);
}
