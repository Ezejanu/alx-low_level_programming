#include "main.h"
/**
 * _abs - a function that computes the absolute value of an integer
 * @a: value input to be evaluated
 * Return: 0 Always (success)
 */
int _abs(int a)
{
	if (a < 0)
	{
		a = a * (-1);
	return (a);
	}
	else if
		(a > 0)
		{
			return (a);
		}
	return (0);
}
