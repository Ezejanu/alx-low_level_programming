#include "main.h"

/**
 * _islower - checks for lowercase character
 *
 * c - int. to be checked
 * Return: 1 if c is lowercase
 * otherwise, return: Always 0 (success)
 */

int _islower(int c)
{
	char a;
	char z;

	if (c >= a && c <= z)
		return (1);
	else
		return (0);
}

